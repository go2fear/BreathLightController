buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2' // Update this to the latest version
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
